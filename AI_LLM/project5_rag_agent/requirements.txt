# Đã kiểm chứng bằng cách cài thật và import thật (2026-08).
#
# CẢNH BÁO ĐÃ KIỂM CHỨNG: `pip install ragas==0.4.3` trên môi trường sạch sẽ kéo về
# langchain 1.x, và khi đó `import ragas` HỎNG NGAY:
#     ModuleNotFoundError: No module named 'langchain_community.chat_models.vertexai'
# (ragas 0.4.3 còn import đường dẫn đã bị xóa ở langchain-community 0.4.)
# Vì vậy bốn dòng langchain* dưới đây BẮT BUỘC phải ghim.
# Chạy `python -m eval.doctor` để tự kiểm tra và in ra lệnh sửa.

# --- truy hồi ---
chromadb>=1.5,<2
sentence-transformers>=3
numpy

# --- agent ---
llama-index-core==0.14.24
llama-index-llms-google-genai==0.10.0    # llama-index-llms-gemini đã ngừng hỗ trợ từ v0.6.2
llama-index-embeddings-huggingface>=0.7
llama-index-vector-stores-chroma>=0.5

# --- đánh giá ---
ragas==0.4.3
langchain<1.0
langchain-core<1.0
langchain-community<0.4
langchain-openai<1.0
instructor>=1.6          # cần cho instructor.from_genai; bản 1.3.x thiếu hàm này
openai>=1.0              # dùng AsyncOpenAI trỏ vào cổng tương thích OpenAI của Gemini
rapidfuzz                # NonLLMContextRecall lỗi ImportError nếu thiếu
pandas                   # result.to_pandas()
datasets

python-dotenv
